# robots.txt — app.oneononehq.com (личный кабинет)
#
# Здесь нет публичных страниц: всё за авторизацией — встречи, задачи, заметки,
# аналитика, база знаний команды, биллинг, а также /api. Индексировать нечего,
# поэтому домен закрыт целиком, для всех роботов без исключений.
#
# Публичная часть продукта — на oneononehq.com, там индексация разрешена,
# в том числе AI-краулерам (см. oneononehq.com/robots.txt и /llms.txt).

User-agent: *
Disallow: /
