# Версии зафиксированы точно. Обновление — только после прогона pip-audit и
# проверки, что схема OpenAPI и поведение эндпоинтов не изменились.
#
# fastapi 0.141.1 подтягивает starlette 1.3.1 — им закрыты уязвимости
# starlette 0.41.3 (подмена Host/пути в request.url, обход лимитов form(),
# квадратичный разбор Range в FileResponse, SSRF в StaticFiles на Windows).
fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.18.5
pydantic==2.13.4
pydantic-settings==2.14.2
celery[redis]==5.4.0
redis==5.2.1
python-dateutil==2.9.0.post0
httpx==0.28.1
# python-multipart 0.0.32 — закрыты отказы в обслуживании при разборе
# multipart/form-data и расхождение с WHATWG в QuerystringParser.
python-multipart==0.0.32
boto3==1.35.99
# pyjwt 2.13.0 — закрыты обход allow-list алгоритмов, игнорирование заголовка
# crit и уязвимости PyJWKClient.
pyjwt==2.13.0
# bcrypt 4.3.0 — последняя версия ветки 4.x. Переход на 5.x отложен: там
# пароль длиннее 72 байт вызывает ValueError вместо усечения, нужен явный
# срез в utils/passwords.py.
bcrypt==4.3.0
openai==2.52.0
